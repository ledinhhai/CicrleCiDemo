 version: 2.1
 
 orbs:
  win: circleci/windows@2.2.0
 
 jobs:
   build_test:
     executor: win/default     
     steps:
      - checkout
      - run: 
          name: build project
          command: dotnet build CicrleCiDemo/CicrleCiDemo.csproj
         
 workflows:
   version: 2
   my_workflows:
    jobs:
     - build_test
