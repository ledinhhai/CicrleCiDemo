 version: 2.1
 
 orbs:
  win: circleci/windows@2.2.0
 
 jobs:
   build:
     executor: win/default     
     steps:
      - checkout
      - run: 
          name: restore 
          command: dotnet restore CicrleCiDemo/CicrleCiDemo.csproj
     
      - run: 
          name: build project
          command: dotnet build CicrleCiDemo/CicrleCiDemo.csproj
      
   deploy:
     machine: # executor type
      image: ubuntu-1604:201903-01
     steps:
      - run:
          command: ssh haild@115.73.217.80 -p 2020 ./deploy.sh
   deploy-window:
     machine: # executor type
      image: ubuntu-1604:201903-01
     steps:
      - add_ssh_keys:
          fingerprints:
            - d2:1d:e1:56:1f:b4:4b:dc:5f:89:9c:79:6d:cf:b6:97
      - run:
          command: ssh nghia@115.73.217.80 -p 21 deploy.bat 
         
 workflows:
   version: 2
   my_workflows:
    jobs:
     - build
     - deploy-window:
         requires:
           - build
